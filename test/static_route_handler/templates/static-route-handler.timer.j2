[Unit]
Description=Check static routes for changes and stores them in git

[Timer]
OnCalendar={{ range(10, 23) | random(seed=inventory_hostname) }}:{{ range(10, 59) | random(seed=inventory_hostname) }}:{{ range(10, 59) | random(seed=inventory_hostname) }}
Persistent=true

[Install]
WantedBy=timers.target
